# Use official Node 16 Alpine base image
FROM node:16-alpine

# Install Docker CLI (run docker build/push inside the agent)
RUN apk add --no-cache docker-cli

# Set work directory for builds
WORKDIR /app